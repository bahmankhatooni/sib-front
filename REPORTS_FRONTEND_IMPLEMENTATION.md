# صفحه گزارشات - پیاده‌سازی فرانت‌اند

## خلاصه تغییرات

صفحه گزارشات (`ReportsPage.vue`) به‌طور کامل بازنویسی شد و به جای داده‌های استاتیک، حالا به APIهای بک‌اند متصل شده است.

---

## فایل‌های ایجاد شده

### 1. Auth Store
**مسیر:** `src/stores/auth.js`

Store مدیریت احراز هویت که شامل:
- مدیریت وضعیت کاربر (user state)
- عملیات login/logout
- بررسی نقش کاربر (admin یا کاربر عادی)
- مدیریت token

---

## فایل‌های اصلاح شده

### 1. ReportsPage.vue
**مسیر:** `src/pages/ReportsPage.vue`

**تغییرات عمده:**
- **حذف داده‌های استاتیک** و اتصال به APIهای بک‌اند
- **فیلترهای پویا** برای تاریخ، واحد، هدف، برنامه و وضعیت
- **آمار زنده** از بک‌اند (تعداد کل، تکمیل شده، درصد تکمیل)
- **نمودارهای داینامیک** بر اساس داده‌های واقعی
- **جدول کاربرگ‌ها** با صفحه‌بندی و فیلتر
- **دیالوگ جزئیات** برای مشاهده کامل اطلاعات کاربرگ
- **دانلود Excel** برای کل گزارش یا تک کاربرگ

---

## ویژگی‌های جدید

### 🔍 فیلترهای پیشرفته
- **بازه تاریخی**: از تاریخ تا تاریخ
- **واحد**: فقط برای ادمین
- **هدف و برنامه**: با ارتباط پویا (انتخاب هدف، برنامه‌ها فیلتر می‌شوند)
- **وضعیت تکمیل**: تکمیل شده / در انتظار
- **جستجو**: در کد کاربرگ

### 📊 آمار و نمودار
- **کارت‌های خلاصه**: تعداد کل، تکمیل شده، ناتمام، درصد تکمیل
- **نمودار میله‌ای**: تعداد کاربرگ‌ها بر اساس واحد
- **نمودار دونات**: توزیع بر اساس اهداف (top 5)

### 📋 جدول کاربرگ‌ها
- **ستون‌ها**: کد، واحد، هدف، برنامه، وضعیت، تاریخ، عملیات
- **کلیک روی کد**: نمایش دیالوگ جزئیات
- **دکمه‌های عملیات**: مشاهده جزئیات، دانلود Excel
- **صفحه‌بندی**: 10، 25، 50 آیتم در صفحه

### 🔍 دیالوگ جزئیات کاربرگ
- **اطلاعات کلی**: تمام فیلدهای ثابت
- **فیلدهای متغیر**: با مقادیر و کاربر سازنده
- **دانلود مستقیم**: Excel آن کاربرگ خاص

### 📥 دانلود Excel
- **گزارش کامل**: بر اساس فیلترهای انتخاب شده
- **تک کاربرگ**: فایل Excel آن کاربرگ با فرمت مطابق الگو

---

## نحوه استفاده

### 1. ورود به صفحه
```
http://your-domain.com/reports
```

### 2. اعمال فیلتر
- تاریخ شروع و پایان را انتخاب کنید
- واحد مورد نظر (اگر ادمین هستید)
- هدف و برنامه
- وضعیت تکمیل
- کلیک "ایجاد گزارش"

### 3. مشاهده آمار
- کارت‌های خلاصه در بالا
- نمودارهای تصویری

### 4. بررسی جدول
- لیست کاربرگ‌های یافت شده
- کلیک روی کد برای جزئیات
- استفاده از دکمه‌های عملیات

### 5. دانلود
- "خروجی Excel" در هدر: کل گزارش
- دکمه دانلود در جدول: تک کاربرگ

---

## دسترسی‌ها

### کاربر عادی (UNIT_RECORDER)
- فقط کاربرگ‌های واحد خودش را می‌بیند
- نمی‌تواند فیلتر واحد را استفاده کند
- آمار فقط مربوط به واحد خودش

### ادمین (ADMIN)
- تمام کاربرگ‌ها را می‌بیند
- می‌تواند بر اساس واحد فیلتر کند
- آمار کلی سیستم

---

## پیش‌نیازها

### 1. تنظیمات بک‌اند
مطمئن شوید که بک‌اند در حال اجرا است:
```bash
cd sib-back
php artisan serve
```

### 2. تنظیمات فرانت‌اند
مطمئن شوید که تنظیمات axios در `boot/axios.js` صحیح است:
```javascript
baseURL: 'http://127.0.0.1:8000/api'
```

### 3. احراز هویت
کاربر باید وارد سیستم شده باشد (token موجود باشد).

---

## ساختار کامپوننت

```javascript
// Data
- statistics (آمار از بک‌اند)
- forms (لیست کاربرگ‌ها)
- units/targets/programs (فیلترها)
- filters (فیلترهای انتخاب شده)

// Methods
- loadReport() (بارگذاری گزارش)
- exportExcel() (دانلود Excel کامل)
- exportSingleForm() (دانلود تک کاربرگ)
- viewFormDetails() (نمایش جزئیات)
- updateCharts() (به‌روزرسانی نمودارها)
```

---

## API Calls

صفحه از این APIها استفاده می‌کند:

```javascript
GET /api/reports/statistics    // آمار کلی
GET /api/reports/list         // لیست کاربرگ‌ها
GET /api/reports/details/{id} // جزئیات کاربرگ
GET /api/reports/export       // دانلود گزارش Excel
GET /api/forms/{id}/export    // دانلود تک کاربرگ
GET /api/units               // لیست واحدها
GET /api/targets             // لیست اهداف
GET /api/programs            // لیست برنامه‌ها
```

---

## کامپوننت‌های استفاده شده

```javascript
- PageHeader (هدر صفحه)
- q-table (جدول Quasar)
- q-dialog (دیالوگ جزئیات)
- q-card، q-btn، q-input، q-select (UI elements)
- Chart.js (نمودارها)
```

---

## استایل‌ها

```scss
- .filter-card (کارت فیلترها)
- .summary-grid (کارت‌های خلاصه)
- .charts-row (نمودارها)
- .table-card (جدول)
- .details-dialog (دیالوگ جزئیات)
```

---

## خطاها و رفع مشکل

### مشکل: آمار نمایش داده نمی‌شود
**علت**: API بک‌اند در دسترس نیست یا token نادرست است  
**راه‌حل**: 
1. بررسی کنید بک‌اند اجرا شده باشد
2. بررسی کنید token در localStorage موجود باشد
3. Console browser را چک کنید

### مشکل: فیلتر واحد نمایش داده نمی‌شود
**علت**: کاربر ادمین نیست  
**راه‌حل**: این طبیعی است، فقط ادمین می‌تواند فیلتر واحد ببیند

### مشکل: دانلود Excel کار نمی‌کند
**علت**: مشکل در responseType یا CORS  
**راه‌حل**: 
1. بررسی کنید `responseType: 'blob'` تنظیم شده باشد
2. بررسی CORS بک‌اند

### مشکل: نمودارها نمایش داده نمی‌شوند
**علت**: Chart.js لود نشده یا داده‌ای موجود نیست  
**راه‌حل**: 
1. بررسی کنید Chart.js import شده باشد
2. بررسی کنید داده‌ای در `statistics.by_unit` موجود باشد

---

## تست کردن

### 1. تست فیلترها
- فیلترهای مختلف را امتحان کنید
- ترکیب چندین فیلتر
- پاک کردن فیلترها

### 2. تست دانلود
- دانلود گزارش کامل
- دانلود تک کاربرگ
- بررسی محتوای فایل Excel

### 3. تست دسترسی‌ها
- ورود با کاربر ادمین
- ورود با کاربر عادی
- بررسی تفاوت دسترسی‌ها

### 4. تست عملکرد
- صفحه‌بندی جدول
- جستجو
- مشاهده جزئیات کاربرگ‌ها

---

## نکات مهم

1. **امنیت**: تمام APIها نیاز به authentication دارند
2. **عملکرد**: فیلترها در سمت بک‌اند اعمال می‌شوند
3. **UX**: Loading states برای بهبود تجربه کاربری
4. **Responsive**: صفحه برای موبایل بهینه شده
5. **مستندات**: کد کاملاً مستند شده

---

## به‌روزرسانی‌های آینده

- [ ] فیلتر بر اساس اقدام و فعالیت
- [ ] نمودارهای بیشتر (خطی، radar)
- [ ] export به PDF
- [ ] گزارش‌های زمان‌بندی شده
- [ ] داشبورد تعاملی

---

صفحه گزارشات آماده استفاده است! 🎉